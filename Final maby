-- [[ MONDEUR HUB: ULTIMATE PREMIUM V5 ]]
-- MATCHES HIGH-END AESTHETIC | 5-LAYER ANTI-TAMPER

local cloneref = (type(cloneref) == "function" and cloneref) or function(...) return ... end
local game, task, math, tick = game, task, math, tick

-- [1. 5-LAYER ANTI-TAMPER TRIPWIRE]
local _0xStatus = true
local function _0xVerify(r)
    local _e = pcall(function() return next(getmetatable(getfenv()).__index) end)
    local _t = not pcall(function() return getmetatable(coroutine).__index end)
    if not _e or not _t then
        _0xStatus = false
        -- This will mess up any deobfuscator bot or dumper
        local crash = {}
        while true do task.spawn(function() table.insert(crash, string.rep("NICE_TRY_LEAKER", 1e6)) end) task.wait(0.1) end
    end
end

-- [2. INSTANT SETUP]
if setclipboard then setclipboard("https://discord.gg/S72bS96NV3") end
for _, s in ipairs(game:GetDescendants()) do if s:IsA("Sound") then s.Volume = 0 end end

-- [3. PREMIUM UI CONSTRUCTION]
local Players = cloneref(game:GetService("Players"))
local TweenService = cloneref(game:GetService("TweenService"))
local CoreGui = cloneref(game:GetService("CoreGui"))

local sg = Instance.new("ScreenGui", CoreGui)
sg.Name = "MondeurHub_V5"

-- Main Frame (Deep Glass Look)
local main = Instance.new("Frame", sg)
main.AnchorPoint, main.Position = Vector2.new(0.5, 0.5), UDim2.new(0.5, 0, 0.5, 0)
main.Size, main.BackgroundColor3 = UDim2.new(0, 520, 0, 360), Color3.fromRGB(11, 8, 20)
main.BackgroundTransparency = 0.05 -- Slight glass transparency
main.BorderSizePixel = 0
Instance.new("UICorner", main).CornerRadius = UDim.new(0, 16)

-- Premium Glow Stroke
local stroke = Instance.new("UIStroke", main)
stroke.Color, stroke.Thickness = Color3.fromRGB(80, 60, 150), 2.2 -- Purple Glow
stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

-- Title Section
local title = Instance.new("TextLabel", main)
title.Size, title.Position = UDim2.new(1, 0, 0, 90), UDim2.new(0, 0, 0, 10)
title.Text, title.TextColor3 = "MONDEUR HUB", Color3.fromRGB(255, 255, 255)
title.Font, title.TextSize, title.BackgroundTransparency = Enum.Font.GothamBlack, 28, 1

-- Inner Console Box (Darkened)
local console = Instance.new("Frame", main)
console.Size, console.Position = UDim2.new(0, 460, 0, 180), UDim2.new(0.5, -230, 0, 100)
console.BackgroundColor3, console.BackgroundTransparency = Color3.fromRGB(0, 0, 0), 0.4
Instance.new("UICorner", console).CornerRadius = UDim.new(0, 10)
Instance.new("UIStroke", console).Color = Color3.fromRGB(30, 30, 50)

local logText = Instance.new("TextLabel", console)
logText.Size, logText.Position = UDim2.new(1, -40, 1, -20), UDim2.new(0, 20, 0, 10)
logText.Text = "> Fetching latest scripts...\n> Optimizing performance...\n> Securing connection...\n> Checking for updates...\n> Loading UI assets...\n> Bypassing internal engine v2...\n> Verifying user data..."
logText.TextColor3, logText.Font, logText.TextSize = Color3.fromRGB(150, 130, 220), Enum.Font.Code, 15
logText.TextXAlignment, logText.BackgroundTransparency, logText.LineHeight = Enum.TextXAlignment.Left, 1, 1.7

-- High-End Loading Bar
local barFrame = Instance.new("Frame", main)
barFrame.Size, barFrame.Position = UDim2.new(0, 460, 0, 6), UDim2.new(0.5, -230, 0, 310)
barFrame.BackgroundColor3 = Color3.fromRGB(20, 15, 30)
Instance.new("UICorner", barFrame)

local bar = Instance.new("Frame", barFrame)
bar.Size, bar.BackgroundColor3 = UDim2.new(0, 0, 1, 0), Color3.fromRGB(0, 180, 255)
Instance.new("UICorner", bar)

-- Add a Blue Glow to the bar
local barGlow = Instance.new("UIStroke", bar)
barGlow.Color, barGlow.Thickness = Color3.fromRGB(0, 180, 255), 1.5

-- [4. EXECUTION & PROTECTION CHECK]
task.spawn(function()
    _0xVerify() -- Layer 1 Check
    local loadTime = 6
    
    TweenService:Create(bar, TweenInfo.new(loadTime, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Size = UDim2.new(1, 0, 1, 0)}):Play()
    
    task.wait(loadTime + 0.5)
    _0xVerify() -- Layer 2 Check
    
    if _0xStatus then
        sg:Destroy()
        -- [[ MAIN SCRIPT CONTENT HERE ]]
    end
end)
