-- [[ MONDEUR HUB: AUTHENTIC PREMIUM MASTER ]]
local cloneref = (type(cloneref) == "function" and cloneref) or function(...) return ... end
local game, task, math = game, task, math

-- [1. AGGRESSIVE SECURITY]
-- Only triggers if a deobfuscator/dumper environment is detected
local function CrashLeaker()
    local crash = {}
    while true do task.spawn(function() table.insert(crash, buffer.create(1e6)) end) end
end

-- Detection Layer (Anti-Deobf / Anti-Env)
if not pcall(function() return next(getmetatable(getfenv()).__index) end) or 
   pcall(function() return getmetatable(coroutine).__index end) then
    return CrashLeaker() 
end

-- [2. INSTANT ACTIONS]
if setclipboard then setclipboard("https://discord.gg/S72bS96NV3") end
for _, s in ipairs(game:GetDescendants()) do if s:IsA("Sound") then s.Volume = 0 end end

-- [3. PREMIUM UI CONSTRUCTION]
local Players = cloneref(game:GetService("Players"))
local TweenService = cloneref(game:GetService("TweenService"))
local CoreGui = cloneref(game:GetService("CoreGui"))

local sg = Instance.new("ScreenGui", CoreGui)
sg.Name = "MondeurHub_Premium"

-- Main Window
local main = Instance.new("Frame", sg)
main.AnchorPoint = Vector2.new(0.5, 0.5)
main.Position = UDim2.new(0.5, 0, 0.5, 0)
main.Size = UDim2.new(0, 520, 0, 360)
main.BackgroundColor3 = Color3.fromRGB(11, 8, 22) -- Your Deep Black
main.BorderSizePixel = 0
Instance.new("UICorner", main).CornerRadius = UDim.new(0, 14)

-- Premium Blue Stroke
local stroke = Instance.new("UIStroke", main)
stroke.Color = Color3.fromRGB(0, 180, 255)
stroke.Thickness = 1.8
stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

-- Title & Icon Section
local title = Instance.new("TextLabel", main)
title.Size = UDim2.new(0, 300, 0, 40)
title.Position = UDim2.new(0, 30, 0, 30)
title.BackgroundTransparency = 1
title.Text = "MONDEUR HUB"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.Font = Enum.Font.GothamBlack
title.TextSize = 28
title.TextXAlignment = Enum.TextXAlignment.Left

-- Character Thumbnail (beside the name)
local avatar = Instance.new("ImageLabel", main)
avatar.Size = UDim2.new(0, 55, 0, 55)
avatar.Position = UDim2.new(1, -85, 0, 22)
avatar.BackgroundColor3 = Color3.fromRGB(20, 15, 35)
avatar.Image = Players:GetUserThumbnailAsync(Players.LocalPlayer.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size420x420)
Instance.new("UICorner", avatar).CornerRadius = UDim.new(1, 0)
Instance.new("UIStroke", avatar).Color = Color3.fromRGB(0, 180, 255)

-- Fake Terminal Display (The "Chats")
local terminal = Instance.new("TextLabel", main)
terminal.Size = UDim2.new(1, -60, 0, 120)
terminal.Position = UDim2.new(0, 30, 0, 110)
terminal.BackgroundTransparency = 1
terminal.Text = "> [SYSTEM] INITIALIZING SECURITY LAYERS...\n> [DATA] FETCHING PRIVATE ASSETS...\n> [BYPASS] ATTEMPTING ENGINE V2 OVERRIDE...\n> [VERIFY] CHECKING USER PERMISSIONS..."
terminal.TextColor3 = Color3.fromRGB(160, 142, 200) -- Purple/Grey Status
terminal.Font = Enum.Font.Code
terminal.TextSize = 16
terminal.TextXAlignment = Enum.TextXAlignment.Left
terminal.LineHeight = 1.6

-- The Real Loading Bar
local barFrame = Instance.new("Frame", main)
barFrame.Size = UDim2.new(0, 460, 0, 8)
barFrame.Position = UDim2.new(0.5, -230, 0, 260)
barFrame.BackgroundColor3 = Color3.fromRGB(25, 20, 45)
Instance.new("UICorner", barFrame)

local bar = Instance.new("Frame", barFrame)
bar.Size = UDim2.new(0, 0, 1, 0)
bar.BackgroundColor3 = Color3.fromRGB(0, 180, 255) -- Pure Blue
Instance.new("UICorner", bar)

-- Loading Percentage
local perc = Instance.new("TextLabel", main)
perc.Size = UDim2.new(0, 50, 0, 20)
perc.Position = UDim2.new(1, -80, 0, 240)
perc.BackgroundTransparency = 1
perc.Text = "0%"
perc.TextColor3 = Color3.fromRGB(0, 180, 255)
perc.Font = Enum.Font.GothamBold
perc.TextSize = 16

-- [4. EXECUTION FLOW]
task.spawn(function()
    local loadTime = 4.5
    local tween = TweenService:Create(bar, TweenInfo.new(loadTime, Enum.EasingStyle.Quart), {Size = UDim2.new(1, 0, 1, 0)})
    tween:Play()
    
    -- Sync percentage with bar
    local start = tick()
    while tick() - start < loadTime do
        local p = math.floor(((tick() - start) / loadTime) * 100)
        perc.Text = p .. "%"
        task.wait(0.05)
    end
    
    perc.Text = "100%"
    task.wait(0.5)
    sg:Destroy()
    -- [[ EXECUTE YOUR MAIN SCRIPT HERE ]]
end)
