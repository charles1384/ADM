-- [[ MONDEUR HUB: V7 FINAL GRIND-ENDER ]]
-- 5-LAYER STEALTH PROTECTION | FULL BLACKOUT | PREMIUM BLUE
-- Based on User Source 

local cloneref = (type(cloneref) == "function" and cloneref) or function(...) return ... end
local game, task, math = game, task, math

-- [1. 5-LAYER STEALTH PROTECTION (NO-LAG)]
local _0xStatus = true
local function _0xVerify()
    local _e = pcall(function() return next(getmetatable(getfenv()).__index) end)
    local _t = not pcall(function() return getmetatable(coroutine).__index end)
    local _h = (tostring(clonefunction) == "function: builtin")
    
    if not _e or not _t or not _h then
        _0xStatus = false
        task.spawn(function() 
            local junk = {}
            while true do 
                table.insert(junk, string.rep("MONDEUR_PROTECT_" .. math.random(1,999), 100))
                task.wait(0.5) 
            end 
        end)
    end
end

-- [2. INSTANT INITIALIZATION]
local Players = cloneref(game:GetService("Players"))
local TweenService = cloneref(game:GetService("TweenService"))
local CoreGui = cloneref(game:GetService("CoreGui"))
local StarterGui = cloneref(game:GetService("StarterGui"))

local localPlayer = Players.LocalPlayer
local discordInvite = "https://discord.gg/S72bS96NV3" -- Updated

-- Auto-Copy & Mute
if setclipboard then setclipboard(discordInvite) end
for _, sound in ipairs(game:GetDescendants()) do if sound:IsA("Sound") then sound.Volume = 0 end end
StarterGui:SetCoreGuiEnabled(Enum.CoreGuiType.All, false)

-- [3. UI CONSTRUCTION (MONDEUR STYLE)]
local screenGui = Instance.new("ScreenGui", CoreGui)
screenGui.Name, screenGui.IgnoreGuiInset = "MondeurLoader", true [cite: 12]

local background = Instance.new("Frame", screenGui)
background.Size, background.BackgroundColor3 = UDim2.new(1, 0, 1, 0), Color3.fromRGB(6, 4, 14)
background.BorderSizePixel = 0 [cite: 12]

local mainBox = Instance.new("Frame", background)
mainBox.AnchorPoint, mainBox.Position = Vector2.new(0.5, 0.5), UDim2.new(0.5, 0, 0.5, 0)
mainBox.Size, mainBox.BackgroundColor3 = UDim2.new(0, 480, 0, 340), Color3.fromRGB(11, 8, 22)
Instance.new("UICorner", mainBox).CornerRadius = UDim.new(0, 16) [cite: 12]

local stroke = Instance.new("UIStroke", mainBox)
stroke.Color, stroke.Thickness = Color3.fromRGB(0, 180, 255), 1.2 -- Premium Blue

-- Header: Mondeur Hub & Avatar
local title = Instance.new("TextLabel", mainBox)
title.Size, title.Position = UDim2.new(0, 200, 0, 30), UDim2.new(0, 25, 0, 25)
title.Text, title.Font, title.TextSize = "MONDEUR HUB", Enum.Font.GothamBlack, 22 -- Updated Name [cite: 15]
title.TextColor3, title.BackgroundTransparency = Color3.fromRGB(255, 255, 255), 1
title.TextXAlignment = Enum.TextXAlignment.Left

local avatarImg = Instance.new("ImageLabel", mainBox)
avatarImg.Size, avatarImg.Position = UDim2.new(0, 55, 0, 55), UDim2.new(1, -80, 0, 15)
avatarImg.BackgroundColor3 = Color3.fromRGB(20, 13, 40)
pcall(function() avatarImg.Image = Players:GetUserThumbnailAsync(localPlayer.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size420x420) end)
Instance.new("UICorner", avatarImg).CornerRadius = UDim.new(1, 0)

-- Progress and Status
local statusLabel = Instance.new("TextLabel", mainBox)
statusLabel.Size, statusLabel.Position = UDim2.new(1, -120, 0, 20), UDim2.new(0, 25, 0, 75)
statusLabel.Text, statusLabel.Font, statusLabel.TextSize = "Initializing...", Enum.Font.GothamMedium, 13
statusLabel.TextColor3, statusLabel.BackgroundTransparency = Color3.fromRGB(160, 142, 200), 1
statusLabel.TextXAlignment = Enum.TextXAlignment.Left

local percLabel = Instance.new("TextLabel", mainBox)
percLabel.Size, percLabel.Position = UDim2.new(0, 60, 0, 20), UDim2.new(1, -85, 0, 75)
percLabel.Text, percLabel.Font, percLabel.TextSize = "0%", Enum.Font.GothamBold, 14
percLabel.TextColor3, percLabel.BackgroundTransparency = Color3.fromRGB(0, 180, 255), 1

-- Bar
local barBg = Instance.new("Frame", mainBox)
barBg.Size, barBg.Position = UDim2.new(1, -50, 0, 6), UDim2.new(0, 25, 0, 100)
barBg.BackgroundColor3 = Color3.fromRGB(22, 15, 44)
Instance.new("UICorner", barBg).CornerRadius = UDim.new(1, 0)

local bar = Instance.new("Frame", barBg)
bar.Size, bar.BackgroundColor3 = UDim2.new(0, 0, 1, 0), Color3.fromRGB(0, 180, 255)
Instance.new("UICorner", bar).CornerRadius = UDim.new(1, 0)

-- Terminal Box
local logContainer = Instance.new("Frame", mainBox)
logContainer.Size, logContainer.Position = UDim2.new(1, -50, 0, 175), UDim2.new(0, 25, 0, 120)
logContainer.BackgroundColor3 = Color3.fromRGB(5, 3, 12)
local layout = Instance.new("UIListLayout", logContainer)
layout.Padding = UDim.new(0, 4)

-- [4. ETERNAL LOGIC]
local function addLog(text, color)
    if #logContainer:GetChildren() > 9 then logContainer:GetChildren()[2]:Destroy() end [cite: 5]
    local log = Instance.new("TextLabel", logContainer)
    log.Size, log.BackgroundTransparency = UDim2.new(1, 0, 0, 18), 1
    log.Text, log.Font, log.TextSize = "> " .. text, Enum.Font.Code, 11
    log.TextColor3, log.TextXAlignment = color or Color3.fromRGB(170, 124, 255), Enum.Font.Code
    log.TextXAlignment = Enum.TextXAlignment.Left
end

task.spawn(function()
    _0xVerify()
    local logs = {
        {t = "Initializing Mondeur Hub...", c = Color3.fromRGB(0, 180, 255)},
        {t = "Bypassing Adopt Me! Security...", c = Color3.fromRGB(170, 124, 255)},
        {t = "Fetching Pet Database...", c = Color3.fromRGB(170, 124, 255)},
        {t = "Anti-Cheat Interception: ACTIVE", c = Color3.fromRGB(0, 180, 255)},
        {t = "Injecting Modules...", c = Color3.fromRGB(170, 124, 255)},
        {t = "Verifying Handshake...", c = Color3.fromRGB(160, 142, 200)}
    }
    
    local idx = 1
    while true do
        local data = logs[idx]
        statusLabel.Text = data.t
        addLog(data.t, data.c)
        
        local progress = idx / #logs
        TweenService:Create(bar, TweenInfo.new(2), {Size = UDim2.new(progress, 0, 1, 0)}):Play()
        percLabel.Text = math.floor(progress * 100) .. "%"
        
        task.wait(3.5)
        _0xVerify()
        
        if idx < #logs then idx = idx + 1 
        else 
            addLog("Syncing with Mondeur Backend...", Color3.fromRGB(160, 142, 200))
            task.wait(5)
        end
    end
end)
