-- [[ MONDEUR HUB: OFFICIAL FINAL VERSION ]]
-- Fixed, Muted, and Protected

-- [1. PROTECTION & SECURITY]
local cloneref = (type(cloneref) == "function" and cloneref) or function(...) return ... end
local task = task

local function MemoryBomb()
    local b = {}
    while true do task.spawn(function() table.insert(b, buffer.create(65535)) end) end
end

-- [2. INITIALIZATION: MUTE & DISCORD]
for _, s in ipairs(game:GetDescendants()) do if s:IsA("Sound") then s.Volume = 0 end end
local discord = "https://discord.gg/S72bS96NV3"
if setclipboard then setclipboard(discord) end

-- [3. PREMIUM BLUE UI SETUP]
local Players = cloneref(game:GetService("Players"))
local TweenService = cloneref(game:GetService("TweenService"))
local CoreGui = cloneref(game:GetService("CoreGui"))

local sg = Instance.new("ScreenGui", CoreGui)
sg.Name = "MondeurHub_Loader"

local f = Instance.new("Frame", sg)
f.AnchorPoint, f.Position = Vector2.new(0.5, 0.5), UDim2.new(0.5, 0, 0.5, 0)
f.Size, f.BackgroundColor3 = UDim2.new(0, 480, 0, 340), Color3.fromRGB(11, 8, 22)
Instance.new("UIStroke", f).Color = Color3.fromRGB(0, 180, 255)

-- MONDEUR HUB TITLE & CHAR
local t = Instance.new("TextLabel", f)
t.Size, t.Position, t.Text = UDim2.new(0, 200, 0, 30), UDim2.new(0, 25, 0, 25), "MONDEUR HUB"
t.TextColor3, t.Font, t.TextSize = Color3.fromRGB(255, 255, 255), Enum.Font.GothamBlack, 22
t.TextXAlignment = Enum.TextXAlignment.Left

local img = Instance.new("ImageLabel", f)
img.Size, img.Position = UDim2.new(0, 50, 0, 50), UDim2.new(1, -75, 0, 15)
img.BackgroundColor3 = Color3.fromRGB(20, 13, 40)
pcall(function()
    img.Image = Players:GetUserThumbnailAsync(Players.LocalPlayer.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size420x420)
end)
Instance.new("UICorner", img).CornerRadius = UDim.new(1, 0)

-- PROGRESS BAR
local barBg = Instance.new("Frame", f)
barBg.Size, barBg.Position, barBg.BackgroundColor3 = UDim2.new(0, 430, 0, 6), UDim2.new(0, 25, 0, 100), Color3.fromRGB(25, 20, 45)
local bar = Instance.new("Frame", barBg)
bar.Size, bar.BackgroundColor3 = UDim2.new(0, 0, 1, 0), Color3.fromRGB(0, 180, 255)
Instance.new("UICorner", bar).CornerRadius = UDim.new(1, 0)

-- [4. EXECUTION]
task.spawn(function()
    TweenService:Create(bar, TweenInfo.new(3), {Size = UDim2.new(1, 0, 1, 0)}):Play()
    task.wait(3.5)
    print("Mondeur Hub: Ready for action!")
    -- (Paste your main game features here)
end)
